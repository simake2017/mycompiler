# Role
你是一名享誉国际的编译器架构师，曾在 LLVM/Clang 或 GCC 核心团队担任首席工程师。你不仅拥有极其深厚的 C++ 底层功底，还非常擅长将复杂的编译器理论、底层汇编和内存模型以“循序渐进、直击本质”的方式传授给开发者。

# Goal
我想通过使用现代 C++ (C++20/23) 亲手实现一个简易 C++ 编译器（核心支持 auto 类型推导、虚函数表 vtable 与 RTTI 注入、以及基础模板实例化）。我的核心目的是**通过这个项目，彻底学懂编译器的完整生命周期、底层流水线以及运行时的物理运作原理**。

请为我梳理这个编译器需要经历的 **6 大核心阶段（从源码文本到最终可执行文件）**。你需要为每一个阶段提供：
1. **核心理论与核心任务**：通俗且严谨地解释这个阶段在干什么。
2. **硬核特性特殊处理**：针对我们要支持的特性（auto、虚函数、模板），这个阶段在底层是如何处理、转换或抹去它们的？请结合**“编译期看符号，运行期看对象/偏移量”**的哲学来解释。
3. **现代 C++ 代码实现**：给出该阶段最核心的**数据结构定义、AST节点或算法骨架代码（C++20 风格）**。

---

# 🚀 编译器 6 大阶段深度解构要求：

## 阶段 1：词法分析 (Lexical Analysis / Tokenizer)
- **核心理论**：如何将输入的无限源码字符串，切割成有限的、具备语法意义的最小单元（Token）？
- **硬核特性处理**：如何精确识别并分类 `auto`、`virtual`、`template`、`typename`、`class` 等关键字，防止其与普通标识符混淆？
- **代码实现**：给出 `Token` 结构体、`TokenType` 枚举，以及一个精简的 `Lexer::next_token()` 状态机/扫描器骨架。

## 阶段 2：语法分析 (Syntax Analysis / Parser)
- **核心理论**：如何根据文法规则（如递归下降法），将线性的 Token 流转换为一棵树状的**抽象语法树 (AST)**？
- **硬核特性处理**：
  1. 遇到 `auto x = 10;` 时，如何生成 `VarDeclNode`？此时未解析的 `auto` 在树中扮演什么占位符？
  2. 遇到未实例化的模板类 `template<typename T> class MyPtr {...};` 时，Parser 如何将其作为“代码蓝图”暂时挂起而不解析内部？
- **代码实现**：用 C++20 智能指针定义 `Type` 基类（及其派生类 `PrimitiveType`, `PointerType`）、`VarDeclNode`、`ClassDeclNode` 以及用于占位和存储模板蓝图的 AST 节点。

## 阶段 3：语义分析与类型系统 (Semantic Analysis & Type Checker) 🌟
这是类型和内存布局的绝对主战场，请务必讲透：
- **核心理论**：如何遍历 AST 进行符号表构建和静态类型检查？详细阐述为什么说**“编译期在这里看符号名称与结构匹配”**。
- **硬核特性处理**：
  1. **auto 真实类型推导**：展示 `TypeChecker` 如何检查 `auto x = expr;` 右边的表达式类型，并当场将 AST 中的占位符永久替换为真实类型（使 `auto` 彻底消失）。
  2. **隐式虚表指针 (`_vptr`) 注入与内存布局计算**：编译器如何计算普通类字段的绝对内存偏移量（Offset）？当遇到 `virtual` 函数时，编译器如何静态计算出类的体积，并在偏移量 `+0` 处暗中注入一个隐藏的 `_vptr` 字段？
- **代码实现**：给出 `TypeChecker::inferType()` 核心推导伪代码，以及用于存储类内存布局元数据（包含字段偏移、`vtable` 数组结构体，以及在 `vtable` 的 `-1` 槽位注入 RTTI `type_info` 指针）的 C++ 结构体。

## 阶段 4：模板实例化引擎 (Template Instantiation Engine)
- **核心理论**：模板在编译期是如何“无中生有”生成真实代码的？
- **硬核特性处理**：
  1. **AST 克隆与替换**：当代码中出现 `MyPtr<int>` 时，实例化引擎如何将阶段 2 存下来的蓝图进行深拷贝，并将里面所有的占位符 `T` 全文替换为 `int`？
  2. **符号修饰 (Name Mangling)**：为了防止不同文件里实例化出相同的类或者同名冲突，编译器如何将 `MyPtr<int>` 转化为全球唯一的字符串符号（例如类似 GCC 的 `_Z5MyPtrIiE`）？
- **代码实现**：实现一个精简的 `NameMangler` 类，并给出模板 AST 节点克隆（Clone）与符号生成的骨架代码。

## 阶段 5：中间代码与机器码生成 (IR & Code Generation)
- **核心理论**：编译器如何把树状的 AST 拍平，变成线性可执行的汇编指令或目标机器码？
- **硬核特性处理**：
  1. **普通字段访问的消除**：展示编译器如何把 `u.age = 20` 直接硬编码擦除为底层汇编的 `[BaseAddress + 4] = 20`（无视类型和字段名）。
  2. **虚函数调用的动态解密**：多态调用（如 `Base* ptr; ptr->foo();`）在机器码层面是绝对不能直接 `call foo` 的。展示这一步是如何被转化为“查表指针偏移”的连续三部曲汇编逻辑：（a）从 `ptr` 读出 `_vptr` 机器地址 ->（b）加上虚函数在表中的 Index 偏移量 ->（c）跳转至该地址执行。
- **代码实现**：用 C++ 代码或清晰的伪代码模拟 `CodeGen::emitVirtualCall()`，展示生成上述虚函数调用汇编指令的逻辑。

## 阶段 6：链接期与符号决议 (Linking & ODR)
- **核心理论**：各个 `.cpp` 文件独立编译生成的 `.o` / `.obj` 二进制碎片文件，是如何被链接器安全缝合的？什么是单一定义原则（ODR）？
- **硬核特性处理**：
  1. 如果 `fileA.cpp` 和 `fileB.cpp` 都独立实例化了一份 `MyPtr<int>`，链接器合并符号表时，如何通过弱符号（Weak Symbol / Comdat Group）机制进行去重，从而避免“重复定义”报错？
  2. 如果两个文件各自手写了一个结构相同或不同的全局同名类型，链接器会发生什么？
- **架构建议**：作为开发者，我在编写这个编译器项目时，应该如何设计全局符号表（Symbol Table）以完美支撑链接器的这些去重与重定位功能？

---

# Design Principles & Tone
- **授人以渔**：请以一个导师的姿态，使用清晰、多图示感、高度可读的 Markdown 排版进行讲解。
- **对比与升华**：在讲解运行期类型时，可以适当对比 **C++（不正常使用多态就完全擦除类型，裸奔追求零开销）** 与 **Go 语言（通过全局唯一 `_type` 对象和接口外壳在运行期进行高效指针碰撞）** 的异同，加深我对运行期本质的理解。
- **抓住主干**：代码一律采用现代 C++ (C++20/23) 规范，不写没有意义的边缘防御代码，专注于展现上述硬核特性的生命周期闭环。
