# ═══════════════════════════════════════════════════════════
# Mini C++ Compiler - x86-64 Assembly Output
# Generated by minicc
# ═══════════════════════════════════════════════════════════

    .text
        .globl main             # 导出函数符号，使链接器可见
    main:                       # 函数入口标签
    # Function: main (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $80, %rsp              # 预留局部变量栈空间（16B 对齐）
    # var a = ...
    movq $5, %rax           # 整数字面量载入 rax
    movq %rax, -16(%rbp)       # 存储到局部变量 a
    # var v1 = ...
    movq -16(%rbp), %rax    # 加载局部变量 a 到 rax
    movq %rax, -24(%rbp)       # 存储到局部变量 v1
    # var v2 = ...
    movq -16(%rbp), %rax    # 加载局部变量 a 到 rax
    movq %rax, -32(%rbp)       # 存储到局部变量 v2
    # var v3 = ...
    leaq -16(%rbp), %rax    # &a = 局部变量栈地址
    movq %rax, -40(%rbp)       # 存储到局部变量 v3
    # var v4 = ...
    movq -16(%rbp), %rax    # 加载局部变量 a 到 rax
    movq %rax, -48(%rbp)       # 存储到局部变量 v4
    # var v5 = ...
    movq -16(%rbp), %rax    # 加载局部变量 a 到 rax
    movq %rax, -56(%rbp)       # 存储到局部变量 v5
    # var v6 = ...
    movq $7, %rax           # 整数字面量载入 rax
    movq %rax, -64(%rbp)       # 存储到局部变量 v6
    # var v7 = ...
    movq -16(%rbp), %rax    # 加载局部变量 a 到 rax
    movq %rax, -72(%rbp)       # 存储到局部变量 v7
    # return expr
    # binary expr
    # binary expr
    # binary expr
    # binary expr
    # binary expr
    movq -24(%rbp), %rax    # 加载局部变量 v1 到 rax
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    movq -32(%rbp), %rax    # 加载局部变量 v2 到 rax
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    addq %rcx, %rax             # 加法：rax = rax + rcx
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    movq -48(%rbp), %rax    # 加载局部变量 v4 到 rax
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    addq %rcx, %rax             # 加法：rax = rax + rcx
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    movq -56(%rbp), %rax    # 加载局部变量 v5 到 rax
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    addq %rcx, %rax             # 加法：rax = rax + rcx
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    movq -64(%rbp), %rax    # 加载局部变量 v6 到 rax
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    addq %rcx, %rax             # 加法：rax = rax + rcx
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    movq -72(%rbp), %rax    # 加载局部变量 v7 到 rax
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    addq %rcx, %rax             # 加法：rax = rax + rcx
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
