# ═══════════════════════════════════════════════════════════
# Mini C++ Compiler - x86-64 Assembly Output
# Generated by minicc
# ═══════════════════════════════════════════════════════════

    .text
        .globl main             # 导出函数符号，使链接器可见
    main:                       # 函数入口标签
    # Function: main (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $112, %rsp              # 预留局部变量栈空间（16B 对齐）
    # stack object a : MyPtr_int (8 bytes, RAII)
    movq $0, -16(%rbp)    # 零初始化 a 偏移 +0
    leaq -16(%rbp), %rdi       # this = 栈对象地址 &a
    callq MyPtr_int_MyPtr_int               # 调用构造函数
    movq $1, %rax           # 整数字面量载入 rax
    # member assign: .value = ...
    leaq -16(%rbp), %rax    # 取栈对象地址 &a
    movq %rax, %rcx                # 对象地址存入 rcx
    movq $1, %rax           # 整数字面量载入 rax
    movl %eax, 0(%rcx)    # 写入字段 .value（偏移 +0）
    # stack object a2 : MyPtr_int (8 bytes, RAII)
    movq $0, -24(%rbp)    # 零初始化 a2 偏移 +0
    leaq -24(%rbp), %rdi       # this = 栈对象地址 &a2
    callq MyPtr_int_MyPtr_int               # 调用构造函数
    movq $2, %rax           # 整数字面量载入 rax
    # member assign: .value = ...
    leaq -24(%rbp), %rax    # 取栈对象地址 &a2
    movq %rax, %rcx                # 对象地址存入 rcx
    movq $2, %rax           # 整数字面量载入 rax
    movl %eax, 0(%rcx)    # 写入字段 .value（偏移 +0）
    # stack object b : MyPtr_int (8 bytes, RAII)
    movq $0, -32(%rbp)    # 零初始化 b 偏移 +0
    leaq -32(%rbp), %rdi       # this = 栈对象地址 &b
    callq MyPtr_int_MyPtr_int               # 调用构造函数
    movq $3, %rax           # 整数字面量载入 rax
    # member assign: .value = ...
    leaq -32(%rbp), %rax    # 取栈对象地址 &b
    movq %rax, %rcx                # 对象地址存入 rcx
    movq $3, %rax           # 整数字面量载入 rax
    movl %eax, 0(%rcx)    # 写入字段 .value（偏移 +0）
    # var acc = ...
    # binary expr
    # binary expr
    # binary expr
    # member access: .value
    leaq -16(%rbp), %rax    # 取栈对象地址 &a
    movl 0(%rax), %eax    # 读取字段 .value（偏移 +0，4B int）
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    movq $100, %rax           # 整数字面量载入 rax
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    imulq %rcx, %rax            # 乘法：rax = rax * rcx（有符号）
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    # binary expr
    # member access: .value
    leaq -24(%rbp), %rax    # 取栈对象地址 &a2
    movl 0(%rax), %eax    # 读取字段 .value（偏移 +0，4B int）
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    movq $10, %rax           # 整数字面量载入 rax
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    imulq %rcx, %rax            # 乘法：rax = rax * rcx（有符号）
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    addq %rcx, %rax             # 加法：rax = rax + rcx
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    # member access: .value
    leaq -32(%rbp), %rax    # 取栈对象地址 &b
    movl 0(%rax), %eax    # 读取字段 .value（偏移 +0，4B int）
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    addq %rcx, %rax             # 加法：rax = rax + rcx
    movq %rax, -40(%rbp)       # 存储到局部变量 acc
    # var y = ...
    movq $4, %rax           # 整数字面量载入 rax
    movq %rax, -48(%rbp)       # 存储到局部变量 y
    # var d = ...
    movq -48(%rbp), %rax    # 加载局部变量 y 到 rax
    movq %rax, -56(%rbp)       # 存储到局部变量 d
    # if condition
    # binary expr
    movq -56(%rbp), %rax    # 加载局部变量 d 到 rax
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    movq $4, %rax           # 整数字面量载入 rax
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    cmpq %rcx, %rax             # 比较：rax - rcx 设置标志位
    setne %al                   # 不等时 al = 1（ZF=0）
    movzbq %al, %rax            # 零扩展 al 到 64 位 rax
    testq %rax, %rax             # 条件值与自身按位与，设置 ZF 标志位
    je endif_1                     # 条件为假（ZF=1）跳转到 else/endif
    # then branch
    # return expr
    movq $91, %rax           # 整数字面量载入 rax
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    endif_1:                        # endif 标签
    # stack object e : Buf_5 (8 bytes, RAII)
    movq $0, -64(%rbp)    # 零初始化 e 偏移 +0
    leaq -64(%rbp), %rdi       # this = 栈对象地址 &e
    callq Buf_5_Buf_5               # 调用构造函数
    movq $5, %rax           # 整数字面量载入 rax
    # member assign: .a = ...
    leaq -64(%rbp), %rax    # 取栈对象地址 &e
    movq %rax, %rcx                # 对象地址存入 rcx
    movq $5, %rax           # 整数字面量载入 rax
    movl %eax, 0(%rcx)    # 写入字段 .a（偏移 +0）
    # if condition
    # binary expr
    # member access: .a
    leaq -64(%rbp), %rax    # 取栈对象地址 &e
    movl 0(%rax), %eax    # 读取字段 .a（偏移 +0，4B int）
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    movq $5, %rax           # 整数字面量载入 rax
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    cmpq %rcx, %rax             # 比较：rax - rcx 设置标志位
    setne %al                   # 不等时 al = 1（ZF=0）
    movzbq %al, %rax            # 零扩展 al 到 64 位 rax
    testq %rax, %rax             # 条件值与自身按位与，设置 ZF 标志位
    je endif_3                     # 条件为假（ZF=1）跳转到 else/endif
    # then branch
    # return expr
    movq $92, %rax           # 整数字面量载入 rax
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    endif_3:                        # endif 标签
    # stack object g : MyPtr_int (8 bytes, RAII)
    movq $0, -72(%rbp)    # 零初始化 g 偏移 +0
    leaq -72(%rbp), %rdi       # this = 栈对象地址 &g
    callq MyPtr_int_MyPtr_int               # 调用构造函数
    movq $7, %rax           # 整数字面量载入 rax
    # member assign: .value = ...
    leaq -72(%rbp), %rax    # 取栈对象地址 &g
    movq %rax, %rcx                # 对象地址存入 rcx
    movq $7, %rax           # 整数字面量载入 rax
    movl %eax, 0(%rcx)    # 写入字段 .value（偏移 +0）
    # stack object h : MyPtr_int (8 bytes, RAII)
    movq $0, -80(%rbp)    # 零初始化 h 偏移 +0
    leaq -80(%rbp), %rdi       # this = 栈对象地址 &h
    callq MyPtr_int_MyPtr_int               # 调用构造函数
    movq $8, %rax           # 整数字面量载入 rax
    # member assign: .value = ...
    leaq -80(%rbp), %rax    # 取栈对象地址 &h
    movq %rax, %rcx                # 对象地址存入 rcx
    movq $8, %rax           # 整数字面量载入 rax
    movl %eax, 0(%rcx)    # 写入字段 .value（偏移 +0）
    # var i = ...
    # function call
    leaq -72(%rbp), %rax    # 取栈对象地址 &g
    pushq %rax                  # 实参值压栈暂存
    popq %rdi                   # 逆序弹出实参到寄存器
    callq _Z6readAtIiE                  # 调用函数 _Z6readAtIiE
    movq %rax, -88(%rbp)       # 存储到局部变量 i
    # var j = ...
    # function call
    leaq -80(%rbp), %rax    # 取栈对象地址 &h
    pushq %rax                  # 实参值压栈暂存
    movq $9, %rax           # 整数字面量载入 rax
    pushq %rax                  # 实参值压栈暂存
    popq %rsi                   # 逆序弹出实参到寄存器
    popq %rdi                   # 逆序弹出实参到寄存器
    callq _Z7writeAtIiE                  # 调用函数 _Z7writeAtIiE
    movq %rax, -96(%rbp)       # 存储到局部变量 j
    # var sum = ...
    # binary expr
    movq -88(%rbp), %rax    # 加载局部变量 i 到 rax
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    movq -96(%rbp), %rax    # 加载局部变量 j 到 rax
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    addq %rcx, %rax             # 加法：rax = rax + rcx
    movq %rax, -104(%rbp)       # 存储到局部变量 sum
    # if condition
    # binary expr
    movq -88(%rbp), %rax    # 加载局部变量 i 到 rax
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    movq $7, %rax           # 整数字面量载入 rax
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    cmpq %rcx, %rax             # 比较：rax - rcx 设置标志位
    setne %al                   # 不等时 al = 1（ZF=0）
    movzbq %al, %rax            # 零扩展 al 到 64 位 rax
    testq %rax, %rax             # 条件值与自身按位与，设置 ZF 标志位
    je endif_5                     # 条件为假（ZF=1）跳转到 else/endif
    # then branch
    # return expr
    movq $93, %rax           # 整数字面量载入 rax
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    endif_5:                        # endif 标签
    # if condition
    # binary expr
    movq -96(%rbp), %rax    # 加载局部变量 j 到 rax
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    movq $9, %rax           # 整数字面量载入 rax
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    cmpq %rcx, %rax             # 比较：rax - rcx 设置标志位
    setne %al                   # 不等时 al = 1（ZF=0）
    movzbq %al, %rax            # 零扩展 al 到 64 位 rax
    testq %rax, %rax             # 条件值与自身按位与，设置 ZF 标志位
    je endif_7                     # 条件为假（ZF=1）跳转到 else/endif
    # then branch
    # return expr
    movq $94, %rax           # 整数字面量载入 rax
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    endif_7:                        # endif 标签
    # if condition
    # binary expr
    # member access: .value
    leaq -80(%rbp), %rax    # 取栈对象地址 &h
    movl 0(%rax), %eax    # 读取字段 .value（偏移 +0，4B int）
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    movq $9, %rax           # 整数字面量载入 rax
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    cmpq %rcx, %rax             # 比较：rax - rcx 设置标志位
    setne %al                   # 不等时 al = 1（ZF=0）
    movzbq %al, %rax            # 零扩展 al 到 64 位 rax
    testq %rax, %rax             # 条件值与自身按位与，设置 ZF 标志位
    je endif_9                     # 条件为假（ZF=1）跳转到 else/endif
    # then branch
    # return expr
    movq $95, %rax           # 整数字面量载入 rax
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    endif_9:                        # endif 标签
    # return expr
    # binary expr
    # binary expr
    # binary expr
    # binary expr
    movq -40(%rbp), %rax    # 加载局部变量 acc 到 rax
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    movq -56(%rbp), %rax    # 加载局部变量 d 到 rax
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    addq %rcx, %rax             # 加法：rax = rax + rcx
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    # member access: .a
    leaq -64(%rbp), %rax    # 取栈对象地址 &e
    movl 0(%rax), %eax    # 读取字段 .a（偏移 +0，4B int）
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    addq %rcx, %rax             # 加法：rax = rax + rcx
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    movq -104(%rbp), %rax    # 加载局部变量 sum 到 rax
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    addq %rcx, %rax             # 加法：rax = rax + rcx
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    movq $25, %rax           # 整数字面量载入 rax
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    addq %rcx, %rax             # 加法：rax = rax + rcx
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    # ~MyPtr_int() auto at function end (RAII)
    leaq -80(%rbp), %rdi       # this = 栈上对象地址（直接取址，无指针变量）
    callq MyPtr_int_dtor            # 静态调用析构函数（非虚析构路径）
    # ~MyPtr_int() auto at function end (RAII)
    leaq -72(%rbp), %rdi       # this = 栈上对象地址（直接取址，无指针变量）
    callq MyPtr_int_dtor            # 静态调用析构函数（非虚析构路径）
    # ~Buf_5() auto at function end (RAII)
    leaq -64(%rbp), %rdi       # this = 栈上对象地址（直接取址，无指针变量）
    callq Buf_5_dtor            # 静态调用析构函数（非虚析构路径）
    # ~MyPtr_int() auto at function end (RAII)
    leaq -32(%rbp), %rdi       # this = 栈上对象地址（直接取址，无指针变量）
    callq MyPtr_int_dtor            # 静态调用析构函数（非虚析构路径）
    # ~MyPtr_int() auto at function end (RAII)
    leaq -24(%rbp), %rdi       # this = 栈上对象地址（直接取址，无指针变量）
    callq MyPtr_int_dtor            # 静态调用析构函数（非虚析构路径）
    # ~MyPtr_int() auto at function end (RAII)
    leaq -16(%rbp), %rdi       # this = 栈上对象地址（直接取址，无指针变量）
    callq MyPtr_int_dtor            # 静态调用析构函数（非虚析构路径）
    
        .globl MyPtr_int_get             # 导出函数符号，使链接器可见
    MyPtr_int_get:                       # 函数入口标签
    # Function: get (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    # return expr
    movq -8(%rbp), %rax    # 加载 this 指针
    movl 0(%rax), %eax    # 读取字段 .value（偏移 +0 字节）
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl MyPtr_int_MyPtr_int             # 导出函数符号，使链接器可见
    MyPtr_int_MyPtr_int:                       # 函数入口标签
    # Function: MyPtr_int (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq -8(%rbp), %rax         # 构造函数返回 this 指针
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl MyPtr_int_dtor             # 导出函数符号，使链接器可见
    MyPtr_int_dtor:                       # 函数入口标签
    # Function: ~MyPtr_int (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq $0, %rax               # void 函数返回 0
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl Buf_5_Buf_5             # 导出函数符号，使链接器可见
    Buf_5_Buf_5:                       # 函数入口标签
    # Function: Buf_5 (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq -8(%rbp), %rax         # 构造函数返回 this 指针
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl Buf_5_dtor             # 导出函数符号，使链接器可见
    Buf_5_dtor:                       # 函数入口标签
    # Function: ~Buf_5 (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq $0, %rax               # void 函数返回 0
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl _Z6readAtIiE             # 导出函数符号，使链接器可见
    _Z6readAtIiE:                       # 函数入口标签
    # Function: readAt (params: 1)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 形参 v 从寄存器 spill 到栈
    # return expr
    # member access: .value
    movq -8(%rbp), %rax    # 加载局部变量 v 到 rax
    movl 0(%rax), %eax    # 读取字段 .value（偏移 +0，4B int）
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl _Z7writeAtIiE             # 导出函数符号，使链接器可见
    _Z7writeAtIiE:                       # 函数入口标签
    # Function: writeAt (params: 2)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 形参 v 从寄存器 spill 到栈
    movq %rsi, -16(%rbp)         # 形参 x 从寄存器 spill 到栈
    movq -16(%rbp), %rax    # 加载局部变量 x 到 rax
    # member assign: .value = ...
    movq -8(%rbp), %rax    # 加载局部变量 v 到 rax
    movq %rax, %rcx                # 对象地址存入 rcx
    movq -16(%rbp), %rax    # 加载局部变量 x 到 rax
    movq %rax, (%rcx)              # 成员赋值（偏移未知，默认偏移 0）
    # return expr
    # member access: .value
    movq -8(%rbp), %rax    # 加载局部变量 v 到 rax
    movq (%rax), %rax            # 成员访问（偏移未知，默认 0）
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
