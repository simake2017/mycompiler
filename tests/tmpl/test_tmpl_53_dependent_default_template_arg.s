# ═══════════════════════════════════════════════════════════
# Mini C++ Compiler - x86-64 Assembly Output
# Generated by minicc
# ═══════════════════════════════════════════════════════════

    .text
        .globl main             # 导出函数符号，使链接器可见
    main:                       # 函数入口标签
    # Function: main (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    # stack object x : Box_int_int (8 bytes, RAII)
    movq $0, -16(%rbp)    # 零初始化 x 偏移 +0
    leaq -16(%rbp), %rdi       # this = 栈对象地址 &x
    callq Box_int_int_Box_int_int               # 调用构造函数
    movq $1, %rax           # 整数字面量载入 rax
    # member assign: .a = ...
    leaq -16(%rbp), %rax    # 取栈对象地址 &x
    movq %rax, %rcx                # 对象地址存入 rcx
    movq $1, %rax           # 整数字面量载入 rax
    movl %eax, 0(%rcx)    # 写入字段 .a（偏移 +0）
    movq $2, %rax           # 整数字面量载入 rax
    # member assign: .b = ...
    leaq -16(%rbp), %rax    # 取栈对象地址 &x
    movq %rax, %rcx                # 对象地址存入 rcx
    movq $2, %rax           # 整数字面量载入 rax
    movl %eax, 4(%rcx)    # 写入字段 .b（偏移 +4）
    # if condition
    # binary expr
    # member access: .a
    leaq -16(%rbp), %rax    # 取栈对象地址 &x
    movl 0(%rax), %eax    # 读取字段 .a（偏移 +0，4B int）
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    movq $1, %rax           # 整数字面量载入 rax
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    cmpq %rcx, %rax             # 比较：rax - rcx 设置标志位
    setne %al                   # 不等时 al = 1（ZF=0）
    movzbq %al, %rax            # 零扩展 al 到 64 位 rax
    testq %rax, %rax             # 条件值与自身按位与，设置 ZF 标志位
    je endif_1                     # 条件为假（ZF=1）跳转到 else/endif
    # then branch
    # return expr
    movq $11, %rax           # 整数字面量载入 rax
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    endif_1:                        # endif 标签
    # if condition
    # binary expr
    # member access: .b
    leaq -16(%rbp), %rax    # 取栈对象地址 &x
    movl 4(%rax), %eax    # 读取字段 .b（偏移 +4，4B int）
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    movq $2, %rax           # 整数字面量载入 rax
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    cmpq %rcx, %rax             # 比较：rax - rcx 设置标志位
    setne %al                   # 不等时 al = 1（ZF=0）
    movzbq %al, %rax            # 零扩展 al 到 64 位 rax
    testq %rax, %rax             # 条件值与自身按位与，设置 ZF 标志位
    je endif_3                     # 条件为假（ZF=1）跳转到 else/endif
    # then branch
    # return expr
    movq $12, %rax           # 整数字面量载入 rax
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    endif_3:                        # endif 标签
    # var v = ...
    movq $7, %rax           # 整数字面量载入 rax
    movq %rax, -24(%rbp)       # 存储到局部变量 v
    # var pv = ...
    leaq -24(%rbp), %rax    # &v = 局部变量栈地址
    movq %rax, -32(%rbp)       # 存储到局部变量 pv
    # stack object q : PtrBox_int_intP (8 bytes, RAII)
    movq $0, -40(%rbp)    # 零初始化 q 偏移 +0
    leaq -40(%rbp), %rdi       # this = 栈对象地址 &q
    callq PtrBox_int_intP_PtrBox_int_intP               # 调用构造函数
    movq -32(%rbp), %rax    # 加载局部变量 pv 到 rax
    # member assign: .p = ...
    leaq -40(%rbp), %rax    # 取栈对象地址 &q
    movq %rax, %rcx                # 对象地址存入 rcx
    movq -32(%rbp), %rax    # 加载局部变量 pv 到 rax
    movq %rax, 0(%rcx)    # 写入字段 .p（偏移 +0，8B）
    # if condition
    # binary expr
    # member access: .p
    leaq -40(%rbp), %rax    # 取栈对象地址 &q
    movq 0(%rax), %rax    # 读取字段 .p（偏移 +0，8B 指针/long）
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    movq -32(%rbp), %rax    # 加载局部变量 pv 到 rax
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    cmpq %rcx, %rax             # 比较：rax - rcx 设置标志位
    setne %al                   # 不等时 al = 1（ZF=0）
    movzbq %al, %rax            # 零扩展 al 到 64 位 rax
    testq %rax, %rax             # 条件值与自身按位与，设置 ZF 标志位
    je endif_5                     # 条件为假（ZF=1）跳转到 else/endif
    # then branch
    # return expr
    movq $21, %rax           # 整数字面量载入 rax
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    endif_5:                        # endif 标签
    # var w = ...
    movq $5, %rax           # 整数字面量载入 rax
    movq %rax, -48(%rbp)       # 存储到局部变量 w
    # stack object t2 : Two_int_intP (16 bytes, RAII)
    movq $0, -64(%rbp)    # 零初始化 t2 偏移 +0
    movq $0, -56(%rbp)    # 零初始化 t2 偏移 +8
    leaq -64(%rbp), %rdi       # this = 栈对象地址 &t2
    callq Two_int_intP_Two_int_intP               # 调用构造函数
    movq $3, %rax           # 整数字面量载入 rax
    # member assign: .a = ...
    leaq -64(%rbp), %rax    # 取栈对象地址 &t2
    movq %rax, %rcx                # 对象地址存入 rcx
    movq $3, %rax           # 整数字面量载入 rax
    movl %eax, 0(%rcx)    # 写入字段 .a（偏移 +0）
    leaq -48(%rbp), %rax    # &w = 局部变量栈地址
    # member assign: .b = ...
    leaq -64(%rbp), %rax    # 取栈对象地址 &t2
    movq %rax, %rcx                # 对象地址存入 rcx
    leaq -48(%rbp), %rax    # &w = 局部变量栈地址
    movq %rax, 8(%rcx)    # 写入字段 .b（偏移 +8，8B）
    # if condition
    # binary expr
    # member access: .a
    leaq -64(%rbp), %rax    # 取栈对象地址 &t2
    movl 0(%rax), %eax    # 读取字段 .a（偏移 +0，4B int）
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    movq $3, %rax           # 整数字面量载入 rax
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    cmpq %rcx, %rax             # 比较：rax - rcx 设置标志位
    setne %al                   # 不等时 al = 1（ZF=0）
    movzbq %al, %rax            # 零扩展 al 到 64 位 rax
    testq %rax, %rax             # 条件值与自身按位与，设置 ZF 标志位
    je endif_7                     # 条件为假（ZF=1）跳转到 else/endif
    # then branch
    # return expr
    movq $31, %rax           # 整数字面量载入 rax
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    endif_7:                        # endif 标签
    # if condition
    # binary expr
    # member access: .b
    leaq -64(%rbp), %rax    # 取栈对象地址 &t2
    movq 8(%rax), %rax    # 读取字段 .b（偏移 +8，8B 指针/long）
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    leaq -48(%rbp), %rax    # &w = 局部变量栈地址
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    cmpq %rcx, %rax             # 比较：rax - rcx 设置标志位
    setne %al                   # 不等时 al = 1（ZF=0）
    movzbq %al, %rax            # 零扩展 al 到 64 位 rax
    testq %rax, %rax             # 条件值与自身按位与，设置 ZF 标志位
    je endif_9                     # 条件为假（ZF=1）跳转到 else/endif
    # then branch
    # return expr
    movq $32, %rax           # 整数字面量载入 rax
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    endif_9:                        # endif 标签
    # return expr
    movq $0, %rax           # 整数字面量载入 rax
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    # ~Two_int_intP() auto at function end (RAII)
    leaq -64(%rbp), %rdi       # this = 栈上对象地址（直接取址，无指针变量）
    callq Two_int_intP_dtor            # 静态调用析构函数（非虚析构路径）
    # ~PtrBox_int_intP() auto at function end (RAII)
    leaq -40(%rbp), %rdi       # this = 栈上对象地址（直接取址，无指针变量）
    callq PtrBox_int_intP_dtor            # 静态调用析构函数（非虚析构路径）
    # ~Box_int_int() auto at function end (RAII)
    leaq -16(%rbp), %rdi       # this = 栈上对象地址（直接取址，无指针变量）
    callq Box_int_int_dtor            # 静态调用析构函数（非虚析构路径）
    
        .globl Box_int_int_Box_int_int             # 导出函数符号，使链接器可见
    Box_int_int_Box_int_int:                       # 函数入口标签
    # Function: Box_int_int (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq -8(%rbp), %rax         # 构造函数返回 this 指针
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl Box_int_int_dtor             # 导出函数符号，使链接器可见
    Box_int_int_dtor:                       # 函数入口标签
    # Function: ~Box_int_int (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq $0, %rax               # void 函数返回 0
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl PtrBox_int_intP_PtrBox_int_intP             # 导出函数符号，使链接器可见
    PtrBox_int_intP_PtrBox_int_intP:                       # 函数入口标签
    # Function: PtrBox_int_intP (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq -8(%rbp), %rax         # 构造函数返回 this 指针
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl PtrBox_int_intP_dtor             # 导出函数符号，使链接器可见
    PtrBox_int_intP_dtor:                       # 函数入口标签
    # Function: ~PtrBox_int_intP (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq $0, %rax               # void 函数返回 0
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl Two_int_intP_Two_int_intP             # 导出函数符号，使链接器可见
    Two_int_intP_Two_int_intP:                       # 函数入口标签
    # Function: Two_int_intP (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq -8(%rbp), %rax         # 构造函数返回 this 指针
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl Two_int_intP_dtor             # 导出函数符号，使链接器可见
    Two_int_intP_dtor:                       # 函数入口标签
    # Function: ~Two_int_intP (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq $0, %rax               # void 函数返回 0
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
