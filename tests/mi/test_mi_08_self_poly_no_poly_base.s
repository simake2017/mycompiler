# ═══════════════════════════════════════════════════════════
# Mini C++ Compiler - x86-64 Assembly Output
# Generated by minicc
# ═══════════════════════════════════════════════════════════

    .text
        .globl B_B             # 导出函数符号，使链接器可见
    B_B:                       # 函数入口标签
    # Function: B (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq -8(%rbp), %rax         # 构造函数返回 this 指针
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl B_dtor             # 导出函数符号，使链接器可见
    B_dtor:                       # 函数入口标签
    # Function: ~B (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq $0, %rax               # void 函数返回 0
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl A_f             # 导出函数符号，使链接器可见
    A_f:                       # 函数入口标签
    # Function: f (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    # return expr
    movq $5, %rax           # 整数字面量载入 rax
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl A_A             # 导出函数符号，使链接器可见
    A_A:                       # 函数入口标签
    # Function: A (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq -8(%rbp), %rax         # 加载 this 指针
    leaq _ZTV1A(%rip), %rcx    # 取 vtable 首地址
    addq $16, %rcx              # 跳过 offset-to-top 与 RTTI，指向 vtable[0]
    movq %rcx, (%rax)           # 安装主 _vptr 到对象首 8 字节
    movq -8(%rbp), %rax         # 构造函数返回 this 指针
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl A_dtor             # 导出函数符号，使链接器可见
    A_dtor:                       # 函数入口标签
    # Function: ~A (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq $0, %rax               # void 函数返回 0
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl X_X             # 导出函数符号，使链接器可见
    X_X:                       # 函数入口标签
    # Function: X (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq -8(%rbp), %rax         # 构造函数返回 this 指针
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl X_dtor             # 导出函数符号，使链接器可见
    X_dtor:                       # 函数入口标签
    # Function: ~X (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq $0, %rax               # void 函数返回 0
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl Y_Y             # 导出函数符号，使链接器可见
    Y_Y:                       # 函数入口标签
    # Function: Y (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq -8(%rbp), %rax         # 构造函数返回 this 指针
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl Y_dtor             # 导出函数符号，使链接器可见
    Y_dtor:                       # 函数入口标签
    # Function: ~Y (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq $0, %rax               # void 函数返回 0
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl Z_h             # 导出函数符号，使链接器可见
    Z_h:                       # 函数入口标签
    # Function: h (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    # return expr
    movq $100, %rax           # 整数字面量载入 rax
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl Z_Z             # 导出函数符号，使链接器可见
    Z_Z:                       # 函数入口标签
    # Function: Z (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq -8(%rbp), %rax         # 加载 this 指针
    leaq _ZTV1Z(%rip), %rcx    # 取 vtable 首地址
    addq $16, %rcx              # 跳过 offset-to-top 与 RTTI，指向 vtable[0]
    movq %rcx, (%rax)           # 安装主 _vptr 到对象首 8 字节
    movq -8(%rbp), %rax         # 构造函数返回 this 指针
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl Z_dtor             # 导出函数符号，使链接器可见
    Z_dtor:                       # 函数入口标签
    # Function: ~Z (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq $0, %rax               # void 函数返回 0
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl main             # 导出函数符号，使链接器可见
    main:                       # 函数入口标签
    # Function: main (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    # var p = ...
    # new A()
    movq $16, %rdi             # malloc 分配大小：16 字节
    callq malloc                  # 调用 malloc 分配堆内存
    pushq %rax                    # 暂存返回的对象指针到栈上
    leaq _ZTV1A(%rip), %rcx    # 取 vtable 首地址
    addq $16, %rcx              # 跳过 offset-to-top 与 RTTI，指向 vtable[0]
    movq (%rsp), %rax           # 从栈上取回对象指针
    movq %rcx, (%rax)           # 安装主 _vptr 到对象首 8 字节
    movq (%rsp), %rdi             # this = 已分配对象指针（栈顶取出）
    callq A_A                 # 调用构造函数 A_A
    popq %rax                     # 弹出对象指针作为 new 表达式返回值
    # end new A()
    movq %rax, -16(%rbp)       # 存储到局部变量 p
    movq $3, %rax           # 整数字面量载入 rax
    # member assign: .name = ...
    movq -16(%rbp), %rax    # 加载局部变量 p 到 rax
    movq %rax, %rcx                # 对象地址存入 rcx
    movq $3, %rax           # 整数字面量载入 rax
    movl %eax, 8(%rcx)    # 写入字段 .name（偏移 +8）
    # var r = ...
    # binary expr
    # function call
    movq -16(%rbp), %rax    # 加载局部变量 p 到 rax
    movq %rax, %rdi            # this = 对象地址（第 0 参数）
    # VIRTUAL CALL: A::f (vtable[0])
    pushq %rdi                   # 暂存 this（对象地址）到栈上保护
    popq %rdi                   # 弹出 this 指针（恢复对象地址）
    # ═══ 虚函数调用 (a)：从对象读出 _vptr ═══
    movq (%rdi), %rax            # 从对象首 8 字节读出 _vptr
    # ═══ 虚函数调用 (b)：从 vtable 加载函数地址 ═══
    movq 0(%rax), %rax       # 从 vtable[0] 读出函数地址（偏移 0）
    # ═══ 虚函数调用 (c)：间接跳转到真实函数 ═══
    callq *%rax                  # 经 vtable 间接调用（跳转到 rax 所指地址）
    # END VIRTUAL CALL A::f
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    # member access: .name
    movq -16(%rbp), %rax    # 加载局部变量 p 到 rax
    movl 8(%rax), %eax    # 读取字段 .name（偏移 +8，4B int）
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    addq %rcx, %rax             # 加法：rax = rax + rcx
    movq %rax, -24(%rbp)       # 存储到局部变量 r
    # var q = ...
    # new Z()
    movq $24, %rdi             # malloc 分配大小：24 字节
    callq malloc                  # 调用 malloc 分配堆内存
    pushq %rax                    # 暂存返回的对象指针到栈上
    leaq _ZTV1Z(%rip), %rcx    # 取 vtable 首地址
    addq $16, %rcx              # 跳过 offset-to-top 与 RTTI，指向 vtable[0]
    movq (%rsp), %rax           # 从栈上取回对象指针
    movq %rcx, (%rax)           # 安装主 _vptr 到对象首 8 字节
    movq (%rsp), %rdi             # this = 已分配对象指针（栈顶取出）
    callq Z_Z                 # 调用构造函数 Z_Z
    popq %rax                     # 弹出对象指针作为 new 表达式返回值
    # end new Z()
    movq %rax, -32(%rbp)       # 存储到局部变量 q
    movq $1, %rax           # 整数字面量载入 rax
    # member assign: .a = ...
    movq -32(%rbp), %rax    # 加载局部变量 q 到 rax
    movq %rax, %rcx                # 对象地址存入 rcx
    movq $1, %rax           # 整数字面量载入 rax
    movl %eax, 8(%rcx)    # 写入字段 .a（偏移 +8）
    movq $2, %rax           # 整数字面量载入 rax
    # member assign: .b = ...
    movq -32(%rbp), %rax    # 加载局部变量 q 到 rax
    movq %rax, %rcx                # 对象地址存入 rcx
    movq $2, %rax           # 整数字面量载入 rax
    movl %eax, 16(%rcx)    # 写入字段 .b（偏移 +16）
    # binary expr
    # binary expr
    # binary expr
    movq -24(%rbp), %rax    # 加载局部变量 r 到 rax
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    # function call
    movq -32(%rbp), %rax    # 加载局部变量 q 到 rax
    movq %rax, %rdi            # this = 对象地址（第 0 参数）
    # VIRTUAL CALL: Z::h (vtable[0])
    pushq %rdi                   # 暂存 this（对象地址）到栈上保护
    popq %rdi                   # 弹出 this 指针（恢复对象地址）
    # ═══ 虚函数调用 (a)：从对象读出 _vptr ═══
    movq (%rdi), %rax            # 从对象首 8 字节读出 _vptr
    # ═══ 虚函数调用 (b)：从 vtable 加载函数地址 ═══
    movq 0(%rax), %rax       # 从 vtable[0] 读出函数地址（偏移 0）
    # ═══ 虚函数调用 (c)：间接跳转到真实函数 ═══
    callq *%rax                  # 经 vtable 间接调用（跳转到 rax 所指地址）
    # END VIRTUAL CALL Z::h
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    addq %rcx, %rax             # 加法：rax = rax + rcx
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    # member access: .a
    movq -32(%rbp), %rax    # 加载局部变量 q 到 rax
    movl 8(%rax), %eax    # 读取字段 .a（偏移 +8，4B int）
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    addq %rcx, %rax             # 加法：rax = rax + rcx
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    # member access: .b
    movq -32(%rbp), %rax    # 加载局部变量 q 到 rax
    movl 16(%rax), %eax    # 读取字段 .b（偏移 +16，4B int）
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    addq %rcx, %rax             # 加法：rax = rax + rcx
    movq %rax, -24(%rbp)       # 赋值局部变量 r
    # return expr
    movq -24(%rbp), %rax    # 加载局部变量 r 到 rax
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    

    .data
    .globl _ZTV1Z             # 导出 vtable 符号
    .align 8                # 8 字节对齐
_ZTV1Z:                        # vtable 标签
    .quad 0                    # offset-to-top = 0（主基类子对象与对象起始重合）
    .quad _ZTI1Z       # RTTI type_info 指针（vtable[-1]）
    .quad Z_h   # vtable[0]: 虚函数 Z_h

    .globl _ZTI1Z             # 导出 RTTI 符号
    .align 8                # 8 字节对齐
_ZTI1Z:                        # typeinfo 标签
    .quad 0                    # type_info vtable = 0（简化版，未链接真实 RTTI）
    .quad .Ltype_name_Z                 # 指向类型名称字符串
    .quad 2                 # 基类计数（MI 计数风格）
    .quad _ZTI1X                 # base[X] typeinfo 指针
    .quad 8                 # base[X] 子对象偏移（字节）
    .quad _ZTI1Y                 # base[Y] typeinfo 指针
    .quad 16                 # base[Y] 子对象偏移（字节）

    .globl _ZTV1A             # 导出 vtable 符号
    .align 8                # 8 字节对齐
_ZTV1A:                        # vtable 标签
    .quad 0                    # offset-to-top = 0（主基类子对象与对象起始重合）
    .quad _ZTI1A       # RTTI type_info 指针（vtable[-1]）
    .quad A_f   # vtable[0]: 虚函数 A_f

    .globl _ZTI1A             # 导出 RTTI 符号
    .align 8                # 8 字节对齐
_ZTI1A:                        # typeinfo 标签
    .quad 0                    # type_info vtable = 0（简化版，未链接真实 RTTI）
    .quad .Ltype_name_A                 # 指向类型名称字符串
    .quad 1                 # 基类计数（MI 计数风格）
    .quad _ZTI1B                 # base[B] typeinfo 指针
    .quad 8                 # base[B] 子对象偏移（字节）

    .globl _ZTI1B             # 导出 RTTI 符号
    .align 8                # 8 字节对齐
_ZTI1B:                        # typeinfo 标签
    .quad 0                    # type_info vtable = 0（简化版，未链接真实 RTTI）
    .quad .Ltype_name_B                 # 指向类型名称字符串
    .quad 0                 # 无基类（基类计数 = 0）

    .globl _ZTI1X             # 导出 RTTI 符号
    .align 8                # 8 字节对齐
_ZTI1X:                        # typeinfo 标签
    .quad 0                    # type_info vtable = 0（简化版，未链接真实 RTTI）
    .quad .Ltype_name_X                 # 指向类型名称字符串
    .quad 0                 # 无基类（基类计数 = 0）

    .globl _ZTI1Y             # 导出 RTTI 符号
    .align 8                # 8 字节对齐
_ZTI1Y:                        # typeinfo 标签
    .quad 0                    # type_info vtable = 0（简化版，未链接真实 RTTI）
    .quad .Ltype_name_Y                 # 指向类型名称字符串
    .quad 0                 # 无基类（基类计数 = 0）


    .section .rodata
.Ltype_name_Z:                     # 类型名称标签
    .string "Z"           # 类型名称字符串
.Ltype_name_A:                     # 类型名称标签
    .string "A"           # 类型名称字符串
.Ltype_name_B:                     # 类型名称标签
    .string "B"           # 类型名称字符串
.Ltype_name_X:                     # 类型名称标签
    .string "X"           # 类型名称字符串
.Ltype_name_Y:                     # 类型名称标签
    .string "Y"           # 类型名称字符串
