# ═══════════════════════════════════════════════════════════
# Mini C++ Compiler - x86-64 Assembly Output
# Generated by minicc
# ═══════════════════════════════════════════════════════════

    .text
        .globl Five_Five             # 导出函数符号，使链接器可见
    Five_Five:                       # 函数入口标签
    # Function: Five (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq -8(%rbp), %rax         # 构造函数返回 this 指针
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl Five_Five_1             # 导出函数符号，使链接器可见
    Five_Five_1:                       # 函数入口标签
    # Function: Five (params: 1)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq %rsi, -16(%rbp)         # 形参 v 从寄存器 spill 到栈
    movq -16(%rbp), %rax    # 加载局部变量 v 到 rax
    movq -8(%rbp), %rcx       # 加载 this 指针
    movl %eax, 8(%rcx)    # 初始化字段 a（偏移 8，4B）
    movq -8(%rbp), %rax         # 构造函数返回 this 指针
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl Five_dtor             # 导出函数符号，使链接器可见
    Five_dtor:                       # 函数入口标签
    # Function: ~Five (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq $0, %rax               # void 函数返回 0
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl Outer_Outer             # 导出函数符号，使链接器可见
    Outer_Outer:                       # 函数入口标签
    # Function: Outer (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq -8(%rbp), %rax         # 构造函数返回 this 指针
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl Outer_Outer_1             # 导出函数符号，使链接器可见
    Outer_Outer_1:                       # 函数入口标签
    # Function: Outer (params: 1)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq %rsi, -16(%rbp)         # 形参 t 从寄存器 spill 到栈
    movq -16(%rbp), %rax    # 加载局部变量 t 到 rax
    movq -8(%rbp), %rcx       # 加载 this 指针
    movl %eax, 0(%rcx)    # 初始化字段 tag（偏移 0，4B）
    movq $7, %rax           # 整数字面量载入 rax
    pushq %rax               # 暂存嵌套构造实参
    popq %rsi              # 逆序弹出实参到寄存器
    movq -8(%rbp), %rdi       # 加载 this 指针
    addq $4, %rdi          # 调整到嵌套子对象 'f'（偏移 4）
    callq Five_Five_1                # 调用嵌套类构造函数
    movq -8(%rbp), %rax         # 构造函数返回 this 指针
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl Outer_dtor             # 导出函数符号，使链接器可见
    Outer_dtor:                       # 函数入口标签
    # Function: ~Outer (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq $0, %rax               # void 函数返回 0
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl main             # 导出函数符号，使链接器可见
    main:                       # 函数入口标签
    # Function: main (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    # var o = ...
    # new Outer()
    movq $20, %rdi             # malloc 分配大小：20 字节
    callq malloc                  # 调用 malloc 分配堆内存
    pushq %rax                    # 暂存返回的对象指针到栈上
    movq $1, %rax           # 整数字面量载入 rax
    pushq %rax                  # 构造实参压栈暂存
    popq %rsi                   # 逆序弹出实参到寄存器
    movq (%rsp), %rdi             # this = 已分配对象指针（栈顶取出）
    callq Outer_Outer_1                 # 调用构造函数 Outer_Outer_1
    popq %rax                     # 弹出对象指针作为 new 表达式返回值
    # end new Outer()
    movq %rax, -16(%rbp)       # 存储到局部变量 o
    movq $2, %rax           # 整数字面量载入 rax
    # member assign: .tail = ...
    movq -16(%rbp), %rax    # 加载局部变量 o 到 rax
    movq %rax, %rcx                # 对象地址存入 rcx
    movq $2, %rax           # 整数字面量载入 rax
    movl %eax, 16(%rcx)    # 写入字段 .tail（偏移 +16）
    # return expr
    # binary expr
    # member access: .a
    # member access: .f
    movq -16(%rbp), %rax    # 加载局部变量 o 到 rax
    leaq 4(%rax), %rax    # 嵌套类字段 .f 取地址（子对象偏移 +4）
    movl 8(%rax), %eax    # 读取字段 .a（偏移 +8，4B int）
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    # member access: .tail
    movq -16(%rbp), %rax    # 加载局部变量 o 到 rax
    movl 16(%rax), %eax    # 读取字段 .tail（偏移 +16，4B int）
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    addq %rcx, %rax             # 加法：rax = rax + rcx
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
