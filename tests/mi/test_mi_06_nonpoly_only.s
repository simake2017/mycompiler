# ═══════════════════════════════════════════════════════════
# Mini C++ Compiler - x86-64 Assembly Output
# Generated by minicc
# ═══════════════════════════════════════════════════════════

    .text
        .globl A_A             # 导出函数符号，使链接器可见
    A_A:                       # 函数入口标签
    # Function: A (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq -8(%rbp), %rax         # 构造函数返回 this 指针
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl A_A_1             # 导出函数符号，使链接器可见
    A_A_1:                       # 函数入口标签
    # Function: A (params: 1)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq %rsi, -16(%rbp)         # 形参 v 从寄存器 spill 到栈
    movq -16(%rbp), %rax    # 加载局部变量 v 到 rax
    movq -8(%rbp), %rcx       # 加载 this 指针
    movl %eax, 0(%rcx)    # 初始化字段 x（偏移 0，4B）
    movq -8(%rbp), %rax         # 构造函数返回 this 指针
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl A_dtor             # 导出函数符号，使链接器可见
    A_dtor:                       # 函数入口标签
    # Function: ~A (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq $0, %rax               # void 函数返回 0
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl B_B             # 导出函数符号，使链接器可见
    B_B:                       # 函数入口标签
    # Function: B (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq -8(%rbp), %rax         # 构造函数返回 this 指针
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl B_B_1             # 导出函数符号，使链接器可见
    B_B_1:                       # 函数入口标签
    # Function: B (params: 1)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq %rsi, -16(%rbp)         # 形参 v 从寄存器 spill 到栈
    movq -16(%rbp), %rax    # 加载局部变量 v 到 rax
    movq -8(%rbp), %rcx       # 加载 this 指针
    movl %eax, 0(%rcx)    # 初始化字段 y（偏移 0，4B）
    movq -8(%rbp), %rax         # 构造函数返回 this 指针
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl B_dtor             # 导出函数符号，使链接器可见
    B_dtor:                       # 函数入口标签
    # Function: ~B (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq $0, %rax               # void 函数返回 0
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl D_D             # 导出函数符号，使链接器可见
    D_D:                       # 函数入口标签
    # Function: D (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq -8(%rbp), %rax         # 构造函数返回 this 指针
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl D_D_3             # 导出函数符号，使链接器可见
    D_D_3:                       # 函数入口标签
    # Function: D (params: 3)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq %rsi, -16(%rbp)         # 形参 a 从寄存器 spill 到栈
    movq %rdx, -24(%rbp)         # 形参 b 从寄存器 spill 到栈
    movq %rcx, -32(%rbp)         # 形参 dv 从寄存器 spill 到栈
    movq -16(%rbp), %rax    # 加载局部变量 a 到 rax
    pushq %rax               # 暂存实参值到栈上
    popq %rsi              # 逆序弹出实参到寄存器
    movq -8(%rbp), %rdi       # 加载 this 指针作为基类构造第 0 参数
    callq A_A_1                # 调用基类构造函数 'A'
    movq -24(%rbp), %rax    # 加载局部变量 b 到 rax
    pushq %rax               # 暂存实参值到栈上
    popq %rsi              # 逆序弹出实参到寄存器
    movq -8(%rbp), %rdi       # 加载 this 指针作为基类构造第 0 参数
    addq $8, %rdi          # 次基类偏移调整（'B'）
    callq B_B_1                # 调用基类构造函数 'B'
    movq -32(%rbp), %rax    # 加载局部变量 dv 到 rax
    movq -8(%rbp), %rcx       # 加载 this 指针
    movl %eax, 16(%rcx)    # 初始化字段 d（偏移 16，4B）
    movq -8(%rbp), %rax         # 构造函数返回 this 指针
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl D_dtor             # 导出函数符号，使链接器可见
    D_dtor:                       # 函数入口标签
    # Function: ~D (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    movq %rdi, -8(%rbp)         # 保存 this 指针到栈槽
    movq $0, %rax               # void 函数返回 0
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
        .globl main             # 导出函数符号，使链接器可见
    main:                       # 函数入口标签
    # Function: main (params: 0)
    pushq %rbp                    # 保存调用者的帧基址到栈上
    movq %rsp, %rbp               # 建立新栈帧：rbp = rsp（此后用 rbp+偏移访问局部）
    subq $64, %rsp              # 预留局部变量栈空间（16B 对齐）
    # var obj = ...
    # new D()
    movq $20, %rdi             # malloc 分配大小：20 字节
    callq malloc                  # 调用 malloc 分配堆内存
    pushq %rax                    # 暂存返回的对象指针到栈上
    movq $10, %rax           # 整数字面量载入 rax
    pushq %rax                  # 构造实参压栈暂存
    movq $20, %rax           # 整数字面量载入 rax
    pushq %rax                  # 构造实参压栈暂存
    movq $30, %rax           # 整数字面量载入 rax
    pushq %rax                  # 构造实参压栈暂存
    popq %rcx                   # 逆序弹出实参到寄存器
    popq %rdx                   # 逆序弹出实参到寄存器
    popq %rsi                   # 逆序弹出实参到寄存器
    movq (%rsp), %rdi             # this = 已分配对象指针（栈顶取出）
    callq D_D_3                 # 调用构造函数 D_D_3
    popq %rax                     # 弹出对象指针作为 new 表达式返回值
    # end new D()
    movq %rax, -16(%rbp)       # 存储到局部变量 obj
    # return expr
    # binary expr
    # binary expr
    # member access: .x
    movq -16(%rbp), %rax    # 加载局部变量 obj 到 rax
    movl 0(%rax), %eax    # 读取字段 .x（偏移 +0，4B int）
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    # member access: .y
    movq -16(%rbp), %rax    # 加载局部变量 obj 到 rax
    movl 8(%rax), %eax    # 读取字段 .y（偏移 +8，4B int）
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    addq %rcx, %rax             # 加法：rax = rax + rcx
    pushq %rax                  # 左操作数压栈暂存（求右值会覆盖 rax）
    # member access: .d
    movq -16(%rbp), %rax    # 加载局部变量 obj 到 rax
    movl 16(%rax), %eax    # 读取字段 .d（偏移 +16，4B int）
    movq %rax, %rcx             # 右操作数从 rax 转移到 rcx
    popq %rax                   # 弹出左操作数回到 rax
    addq %rcx, %rax             # 加法：rax = rax + rcx
    leave                         # 恢复栈帧（movq %rbp,%rsp; popq %rbp）
    ret                           # 返回调用者（从栈上弹出返回地址）
    
